/* =========================================================
   APPLICATION PARAMETERS / 애플리케이션 설정값
========================================================= */

export const params = {
    treeCount: 10,
    cowCount: 6,
    hour: 12,
    auto: false
};